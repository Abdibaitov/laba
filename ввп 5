 include<iostream> // подключаем библиотеку
#include<math.h>
using namespace std;
int main() {
    void task1(),task2();

    void task1(); {
        setlocale(LC_ALL, "Rus");
        int x1, y1, x2, y2;
        using namespace std;
        cout << "Вводите х1 у1 х2 и у2 " << endl;
        cin >> x1 >> y1 >> x2 >> y2;
        cout << "x1=" << x1 << "\ny1=" << y1 << "\nx2=" << x2 << "\ny2=" << y2 << endl;
        cout << sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
        int task1();
    }
}
void task2() {
    int a, b, c;
     }
